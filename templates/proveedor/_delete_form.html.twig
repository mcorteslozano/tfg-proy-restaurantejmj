<form method="post" action="{{ path('proveedor_delete', {'id': proveedor.id}) }}" onsubmit="return confirm('¿Está seguro que quiere eliminar el proveedor {{ proveedor.id }}?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proveedor.id) }}">
    <button class="btn">Eliminar</button>
</form>
