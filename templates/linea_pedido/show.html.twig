{% extends 'base.html.twig' %}

{% block title %}Gestiona JMJ{% endblock %}

{% block body %}
    <h1>LineaPedido</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ linea_pedido.id }}</td>
            </tr>
            <tr>
                <th>Unidades</th>
                <td>{{ linea_pedido.unidades }}</td>
            </tr>
            <tr>
                <th>Precio_linea</th>
                <td>{{ linea_pedido.precioLinea }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('linea_pedido_index') }}">back to list</a>

    <a href="{{ path('linea_pedido_edit', {'id': linea_pedido.id}) }}">edit</a>

    {{ include('linea_pedido/_delete_form.html.twig') }}
{% endblock %}
